CREATE TABLE STAGIAIRES (ID_STAGIAIRE INTEGER PRIMARY KEY AUTOINCREMENT, NOM TEXT NOT NULL, PRENOM TEXT NOT NULL, EMAIL TEXT NOT NULL UNIQUE, TELEPHONE TEXT NOT NULL, ADRESSE TEXT NOT NULL, DATE_DE_NAISSANCE DATE NOT NULL, SEXE TEXT NOT NULL, CIN TEXT NOT NULL UNIQUE, NIVEAU TEXT NOT NULL);
